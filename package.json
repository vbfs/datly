{
  "name": "datly",
  "version": "0.0.1",
  "description": "",
  "license": "ISC",
  "author": "Vinícis Barreto",
  "type": "module",
  "main": "dist/datly.umd.js",
  "scripts": {
    "build": "microbundle build --entry src/index.js --output dist/datly.js --name datly --format umd --compress --no-sourcemap",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "microbundle": "^0.15.1"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
